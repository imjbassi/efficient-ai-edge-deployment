\section{Conclusion}
\label{sec:conclusion}

INT8 was reliably smaller, but it was not intrinsically faster or accurate. In this MobileNetV2 audit, per-channel quantization delivered useful accuracy and a 1.27$\times$ one-thread XNNPACK speedup, while per-tensor quantization destroyed accuracy and TensorFlow Lite's built-in counterfactual made INT8 slower than FP32. The packaged artifact then exposed the rule in practice: its deployed INT8 graph received zero XNNPACK partitions. The result replaces a blanket ``quantization accelerates inference'' narrative with a testable deployment rule: validate the model's granularity and benchmark the exact backend, thread policy, and serving boundary that will run in production.

The repository turns that rule into a repeatable audit. It retains per-image paired predictions, process-level latency replicates, raw request timings, hashes, host controls, a hardened container, and the scripts that generated the tables and figure. Stronger external validity now requires replication across additional architectures, full ImageNet, native ARM systems, and energy-controlled edge hardware rather than additional claims from the present host.

\section*{Data and Code Availability}

Version 1.2.0 of the code, models, scripts, results, and manuscript is archived at \href{https://doi.org/10.5281/zenodo.22758817}{doi:10.5281/zenodo.22758817}; the matching source release is at \url{https://github.com/imjbassi/efficient-ai-edge-deployment/releases/tag/v1.2.0}. Code and documentation are MIT licensed; TensorFlow/Keras are Apache-2.0, while converted parameters and Imagenette retain the upstream terms documented in the artifact.

\section*{Author Contributions}

J. Bassi performed the conceptualization, methodology, software, investigation, data curation, visualization, and writing.
